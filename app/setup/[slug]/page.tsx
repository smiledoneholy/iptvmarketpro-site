import type{Metadata}from"next";import{notFound}from"next/navigation";import SiteHeader from"../../site-header";import{setupGuides}from"../../content-data";
export function generateStaticParams(){return setupGuides.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,g=setupGuides.find(x=>x.slug===slug);return g?{title:`How to set up ${g.name}`,description:g.summary,alternates:{canonical:`/setup/${g.slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,g=setupGuides.find(x=>x.slug===slug);if(!g)notFound();return <main><SiteHeader active="setup"/><section className="detail-hero setup-detail"><a href="/setup">← All setup guides</a><p className="eyebrow light"><span/> Device tutorial</p><h1>Set up<br/><em>{g.name}.</em></h1><p>{g.summary}. Follow the steps below and use only content you are authorised to access.</p></section><article className="tutorial"><aside><b>Before you start</b><p>Have your device, a stable connection and your authorised access details ready. Never publish your device key or playlist link.</p><a href="/contact">Need help? →</a></aside><ol>{g.steps.map((s,i)=><li key={s}><b>{String(i+1).padStart(2,"0")}</b><div><h2>{["Prepare your device","Install the player","Enter your details","Refresh the service","Test and finish"][i]}</h2><p>{s}</p></div></li>)}</ol></article><section className="detail-cta"><h2>Setup complete?</h2><p>Start with a standard-quality stream, then adjust quality to suit your device and connection.</p><a className="button" href="/channels">Explore channel categories</a></section><Footer/></main>}
function Footer(){return <footer><a className="brand" href="/"><span className="brand-mark"><img src="/market-pro-logo.png" alt="Market Pro logo"/></span><span>MARKET <b>PRO</b></span></a><p>A simple, smooth and supported TV experience.</p><div><a href="/channels">Channel list</a><a href="/setup">Set up</a><a href="/blog">Blog</a></div><small>© 2026 Market Pro. Use this service only for authorised content.</small></footer>}
