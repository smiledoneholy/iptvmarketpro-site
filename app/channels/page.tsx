import SiteFooter from "../site-footer";
import type{Metadata}from"next";import SiteHeader from"../site-header";import{channelRegions}from"../content-data";
export const metadata:Metadata={title:"Channel List",description:"Explore Market Pro channel categories by region, language and content type.",alternates:{canonical:"/channels"}};
export default function Channels(){return <main id="main-content"><SiteHeader active="channels"/><section className="inner-hero"><p className="eyebrow light"><span/> Explore the catalogue</p><h1>Channel <em>list.</em></h1><p>Browse a broad selection organised by country, language and category. Availability may change as the catalogue is updated.</p></section><section className="catalogue section"><div className="section-heading"><div><p className="eyebrow"><span/> Find your favourites</p><h2>Made for a<br/><em>world of choice.</em></h2></div><p>Select a region to open its complete category overview.</p></div><div className="region-grid">{channelRegions.map(r=><a className="region-card" href={`/channels/${r.slug}`} key={r.name}><article><div className="region-top"><span>{r.name}</span></div><p>{r.intro}</p><strong>Explore categories →</strong></article></a>)}</div></section><section className="legal-strip"><b>Important</b><p>Market Pro is intended only for content that you are legally authorised to access. Channel availability is not guaranteed and may vary by region.</p><a href="/contact">Ask about a channel →</a></section><Footer/></main>}
function Footer(){return <SiteFooter/>}
