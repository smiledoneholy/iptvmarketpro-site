import SiteFooter from "../../site-footer";
import type{Metadata}from"next";import{notFound}from"next/navigation";import SiteHeader from"../../site-header";import{channelRegions}from"../../content-data";
export function generateStaticParams(){return channelRegions.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,r=channelRegions.find(x=>x.slug===slug);return r?{title:`${r.name} Channel Categories`,description:r.intro,alternates:{canonical:`/channels/${r.slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,r=channelRegions.find(x=>x.slug===slug);if(!r)notFound();return <main id="main-content"><SiteHeader active="channels"/><section className="detail-hero"><a href="/channels">← Channel list</a><p className="eyebrow light"><span/> Region catalogue</p><h1>{r.name}<br/><em>Channel categories</em></h1><p>{r.intro}</p></section><section className="section detail-content"><div><p className="eyebrow"><span/> Included categories</p><h2>What you can<br/><em>explore.</em></h2><p>Categories are shown for orientation. Exact channel availability can change and should be confirmed before ordering.</p></div><div className="detail-list">{r.groups.map((g,i)=><article key={g}><b>{String(i+1).padStart(2,"0")}</b><div><h3>{g}</h3><p>Browse live and scheduled programming in this category, subject to regional availability and authorised access.</p></div></article>)}</div></section><section className="detail-cta"><h2>Looking for a specific channel?</h2><p>Ask our team to check current availability before choosing a plan.</p><a className="button" href="/contact">Contact support</a></section><Footer/></main>}
function Footer(){return <SiteFooter/>}
