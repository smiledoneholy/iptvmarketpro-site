import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import SiteHeader from '../../site-header';
import SiteFooter from '../../site-footer';
import {blogPosts} from '../../content-data';
export function generateStaticParams(){return blogPosts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=blogPosts.find(p=>p.slug===slug);if(!p)return {};const image=`https://iptvmarketpro.com/${p.image}.webp`;return {title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${p.slug}`},openGraph:{title:p.title,description:p.excerpt,type:'article',url:`https://iptvmarketpro.com/blog/${p.slug}`,images:[image],modifiedTime:p.updated},twitter:{card:'summary_large_image',title:p.title,description:p.excerpt,images:[image]}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=blogPosts.find(p=>p.slug===slug);if(!p)notFound();const url=`https://iptvmarketpro.com/blog/${p.slug}`;const schema={'@context':'https://schema.org','@graph':[{'@type':'Article',headline:p.title,description:p.excerpt,dateModified:p.updated,author:{'@type':'Organization',name:'Market Pro',url:'https://iptvmarketpro.com/about-us'},publisher:{'@type':'Organization',name:'Market Pro'},mainEntityOfPage:url,image:`https://iptvmarketpro.com/${p.image}.webp`,inLanguage:'en'},{'@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:'https://iptvmarketpro.com/'},{'@type':'ListItem',position:2,name:'Guides',item:'https://iptvmarketpro.com/blog'},{'@type':'ListItem',position:3,name:p.title,item:url}]}]};return <main id="main-content"><SiteHeader active="blog"/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,'\\u003c')}}/><header className="article-hero"><a href="/blog">← All guides</a><small>{p.tag} · {p.read}</small><h1>{p.title}</h1><p>{p.excerpt}</p><p className="article-byline">Market Pro · Updated <time dateTime={p.updated}>{p.updated}</time></p></header><article className="article-body" data-no-translate><img className="article-cover" src={`/${p.image}.webp`} srcSet={`/${p.image}-small.webp 640w, /${p.image}.webp 1200w`} sizes="(max-width: 920px) 100vw, 850px" width="1200" height="800" alt={p.imageAlt} loading="lazy"/><nav className="article-toc" aria-label="In this guide"><strong>In this guide</strong><ol>{p.sections.map((s,i)=><li key={s.h}><a href={`#step-${i+1}`}>{s.h}</a></li>)}</ol></nav>{p.sections.map((s,i)=><section id={`step-${i+1}`} key={s.h}><span>{String(i+1).padStart(2,'0')}</span><div><h2>{s.h}</h2><p>{s.p}</p></div></section>)}{p.sources.length>0&&<aside><h2>Sources and further reading</h2><ul>{p.sources.map(s=><li key={s.url}><a href={s.url} rel="noreferrer">{s.label}</a></li>)}</ul><p>Product-specific guidance applies to the named product; it is not a guarantee for every streaming service.</p></aside>}<aside><strong>About this guide</strong><p>Published by Market Pro to help with setup and buying decisions. General guidance, not a hands-on product review. Illustrations generated for this site. <a href="/contact">Tell us if a step needs updating</a>.</p></aside></article><section className="detail-cta"><h2>Find the right setup.</h2><p>Check your device with support before choosing a plan.</p><a className="button" href="/before-you-order">Before you order</a></section><section className="section related-guides"><h2>Keep reading</h2><div className="benefit-grid">{blogPosts.filter(x=>x.slug!==p.slug).slice(0,3).map(x=><article key={x.slug}><h3><a href={`/blog/${x.slug}`}>{x.title}</a></h3><p>{x.excerpt}</p></article>)}</div></section><SiteFooter/></main>}
