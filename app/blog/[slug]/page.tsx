import type{Metadata}from"next";import{notFound}from"next/navigation";import SiteHeader from"../../site-header";import{blogPosts}from"../../content-data";
export function generateStaticParams(){return blogPosts.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params,p=blogPosts.find(x=>x.slug===slug);return p?{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${p.slug}`},openGraph:{title:p.title,description:p.excerpt,type:"article"}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,p=blogPosts.find(x=>x.slug===slug);if(!p)notFound();return <main><SiteHeader active="blog"/><section className="article-hero"><a href="/blog">← Back to blog</a><small>{p.tag} · {p.read}</small><h1>{p.title}</h1><p>{p.excerpt}</p></section><article className="article-body"><div className="article-intro"><b>Market Pro guide</b><p>Practical information designed to help you make informed, responsible choices.</p></div>{p.sections.map((s,i)=><section key={s.h}><span>{String(i+1).padStart(2,"0")}</span><div><h2>{s.h}</h2><p>{s.p}</p></div></section>)}<aside><strong>Responsible use</strong><p>Use media players and online TV services only for content you are legally authorised to access. This article is general information, not legal advice.</p></aside></article><section className="detail-cta"><h2>Need more help?</h2><p>Browse the setup library or contact our support team.</p><a className="button" href="/setup">View setup guides</a></section><Footer/></main>}
function Footer(){return <footer><a className="brand" href="/"><span className="brand-mark"><img src="/market-pro-logo.png" alt="Market Pro logo"/></span><span>MARKET <b>PRO</b></span></a><p>A simple, smooth and supported TV experience.</p><div><a href="/channels">Channel list</a><a href="/setup">Set up</a><a href="/blog">Blog</a></div><small>© 2026 Market Pro. Use this service only for authorised content.</small></footer>}
